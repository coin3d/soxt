TODO
====

* Document the SOXT_JOYSTICK_DEVICE environment variable
* The SoXtLinuxJoystick support is now enabled for every Linux system.
  It is likely that with some changes it could be even be introduced in
  the common device (SoGui sub repo)
